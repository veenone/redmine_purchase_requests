# Plugin-level Gemfile for redmine_purchase_requests
#
# Run from the Redmine root after installing/updating this plugin:
#   bundle install
#
# Redmine's plugin loader picks up plugin Gemfiles automatically.

gem 'caracal', '~> 1.4'  # DOCX report generation
