# Plugin-level Gemfile for redmine_purchase_requests
#
# Currently empty — DocxReportHelper uses Redmine's bundled rubyzip + Nokogiri
# instead of pulling in a DOCX gem (caracal/sablon pin rubyzip ~> 1.x which
# conflicts with Redmine 6's rubyzip ~> 2.3).
