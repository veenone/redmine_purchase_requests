  <!-- TPC Code Grouping -->
  <%= render 'shared/budget_grouping',
        grouping: @tpc_grouping,
        heading: l(:label_budget_by_tpc_code),
        title_for: ->(tpc_code) { content_tag(:span, tpc_code, class: 'tpc-number') } %>
