<% if @template.errors.any? %>
// Show validation errors in the modal
(function() {
  var html = '<%= j render(partial: "form_modal", locals: { title: l(:label_edit_workflow_template, default: "Edit Workflow Template") }) %>';

  var modal = document.getElementById('workflow-template-modal');
  if (modal) {
    modal.innerHTML = html;
    // Focus on the first input
    var firstInput = modal.querySelector('input[type="text"]');
    if (firstInput) {
      firstInput.focus();
    }
  }
})();
<% else %>
// Template updated successfully - reload the page to show the changes
window.location.href = '<%= plugin_settings_path("redmine_purchase_requests", tab: "workflow") %>';
<% end %>
